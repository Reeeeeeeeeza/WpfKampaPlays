<Window x:Class="databindingsss.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:databindingsss"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TextBox Text="{Binding BoundText, UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource }"   Height="50" VerticalAlignment="Top" Background="Green" Foreground="White" Width="500"></TextBox>
        <Button Click="btnSelect_Click" Height="50" Width="200" Content="Press" FontSize="32"></Button>
        <TextBlock Text="{Binding BoundText}" Height="50" Width="500" VerticalAlignment="Bottom" Background="Bisque" ></TextBlock>
    </Grid>
</Window>
