<Window x:Class="listviewobarr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:listviewobarr"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="0" Name="textboxx"></TextBox>
        <Button Grid.Column="1" Click="Add_btn_Click"    Name="Add_btn"       Content="Add"       Width="50"  HorizontalAlignment="Left"></Button>
        <Button Grid.Column="1" Click="Del_btn_Click"    Name="Del_btn"       Content="Delete"    Width="50"  HorizontalAlignment="Center"></Button>
        <Button Grid.Column="1" Click="Clr_btn_Click"   Name="Clr_btn"       Content="Clear"     Width="50"  HorizontalAlignment="Right" ></Button>
        <ListView Name="lv" Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding Entries  }"></ListView>

    </Grid>
</Window>
